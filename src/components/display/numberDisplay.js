import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.View`
  display: flex;
`

export const NumberDisplay = ({setNumberFromDisplay}) => {
  return (
    <Wrapper/>
  )
}

export default NumberDisplay
